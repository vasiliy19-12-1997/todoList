import { FC } from "react";
import "./loader.module.scss";
const Loader: FC = () => {
  return (
    <div className="Loader">
      <h2 className="LoaderHeader">Loading</h2>
    </div>
  );
};

export default Loader;
